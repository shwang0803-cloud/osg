# app.py
# 🧠 OSD: Open Source Design Project
# AI 기반 결제 카드 디자인 생성 시뮬레이터
# 작성자: shwang0803-cloud
# 버전: 1.0

import random
import time

# -------------------------------
# 1️⃣ 카드 디자인 데이터베이스
# -------------------------------
colors = ["블랙", "그레이", "화이트", "네이비", "실버"]
fonts = ["산세리프", "모던 고딕", "캘리그래피", "라운드체"]
patterns = ["미니멀", "라인 아트", "그라데이션", "기하학", "심플 패턴"]

# -------------------------------
# 2️⃣ 디자인 생성 로직
# -------------------------------
def generate_design(user_request):
    """
    사용자 입력을 바탕으로 카드 디자인을 생성하는 함수 (시뮬레이션)
    """
    print("\n🎨 AI가 사용자의 요구를 분석 중입니다...")
    time.sleep(1.5)

    # 간단한 조건 기반 로직
    mood = "감각적인" if "모던" in user_request or "심플" in user_request else "독창적인"
    color_choice = random.choice(colors)
    font_choice = random.choice(fonts)
    pattern_choice = random.choice(patterns)

    print(f"\n🧩 추천 조합 결과:")
    print(f"   • 스타일: {mood} 디자인")
    print(f"   • 색상: {color_choice}")
    print(f"   • 폰트: {font_choice}")
    print(f"   • 패턴: {pattern_choice}")

    print("\n💡 디자인 설명:")
    print(f"'{color_choice}' 색상 배경에 '{pattern_choice}' 패턴을 적용하고, "
          f"'{font_choice}' 폰트를 사용하여 {mood} 느낌의 카드를 구성합니다.")

    print("\n📷 AI가 이미지를 생성 중...")
    time.sleep(1.2)
    print("✅ 'card_design_preview.png' 미리보기 이미지가 가상으로 생성되었습니다.")
    print("💾 card_design.png 파일로 저장 완료 (시뮬레이션)\n")

# -------------------------------
# 3️⃣ 대화형 실행 로직
# -------------------------------
def main():
    print("===================================")
    print("🧠 OSD: AI 카드 디자인 생성기 v1.0")
    print("===================================\n")
    print("💬 원하는 카드 디자인 스타일을 입력해주세요.")
    print("   예) '모던하고 심플한 카드', '감성적인 디자인', '럭셔리한 카드' 등\n")

    while True:
        user_input = input("👉 입력: ")
        if user_input.lower() in ["exit", "종료", "quit"]:
            print("\n👋 프로그램을 종료합니다. 이용해 주셔서 감사합니다!")
            break
        generate_design(user_input)

# -------------------------------
# 4️⃣ 프로그램 실행
# -------------------------------
if __name__ == "__main__":
    main()